let chalk;_e27‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);
const SHA256 = require('crypto-js/sha256');
class Block{
	constructor(index,/*current_time,*/ data, PreviousHash=" "){
	this.index = index;
	this.current_time = Date.now();
	this.data = data;
	this.PreviousHash = PreviousHash;
	this.hash = this.createHash();
	}
	createHash() {
		let hashCreated = SHA256(this.data + this.index + this.PreviousHash + this.current_time + JSON.stringify(this.data));
		return hashCreated;
	}
}

class BlockChain {
	constructor() {
		this.blockchain = [this.createFirstBlock()]
	};
	createFirstBlock() {
		return new Block(0,"First Block", "0");
	}

}

let blockchainObj = new BlockChain()
let a = new Block(1,{from: "Joe", to: "Jane"}, PreviousHash = "0")
let b = new Block(2,{from: "Jane", to: "Joe"}, PreviousHash = a.hash)
